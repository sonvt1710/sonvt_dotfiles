# Commands

Fill in the actual commands for this project.

## Install

```bash
# e.g. pip install -r requirements.txt
```

## Development

```bash
# e.g. python main.py
```

## Lint

```bash
# e.g. ruff check .
```

## Test

```bash
# e.g. pytest
```

## Build

```bash
# e.g. python -m build
```

## Database

```bash
# e.g. alembic upgrade head  /  pnpm db:migrate
# e.g. pnpm db:seed
```

## Docker

```bash
# docker compose up -d
# docker compose logs -f
```

## Notes

- Do not run destructive commands without approval.
- Prefer targeted tests when working on one module.
